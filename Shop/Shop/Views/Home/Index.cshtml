

<div class="row mt-5 mb-2">
    @{
        foreach (var category in Model.AllCategories)
        {
            <div class="col-lg-4">
                <h2>@category.Name</h2>
            </div>
        }
    }
</div>